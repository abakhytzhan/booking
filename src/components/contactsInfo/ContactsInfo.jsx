import { Typography } from "@mui/material";

const ContactsInfo = () => {
  return (
    <Typography
      variant="h4"
      sx={{
        fontFamily: "monospace",
        fontWeight: 900,
        letterSpacing: ".3rem",
        color: "primary.dark",
        textAlign: "center",
      }}
    >
      CONTACTS
    </Typography>
  );
};

export default ContactsInfo;
