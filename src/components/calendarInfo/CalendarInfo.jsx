import { Typography } from "@mui/material";

const CalendarInfo = () => {
  return (
    <Typography
      variant="h4"
      sx={{
        fontFamily: "monospace",
        fontWeight: 900,
        letterSpacing: ".3rem",
        color: "primary.dark",
        textAlign: "center",
      }}
    >
      CALENDAR
    </Typography>
  );
};

export default CalendarInfo;
